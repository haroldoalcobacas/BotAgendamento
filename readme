
## 🚀 BootAgendamento

🎯 Objetivo do Projeto

O BootAgendamento automatiza o agendamento de estúdios e salas, eliminando a necessidade de atendimento manual via telefone ou e-mail.
Ele oferece uma agenda robusta, garantindo que não haja conflitos de horário entre múltiplos recursos (salas) do estúdio.


## 🛠️Funcionalidades

- 💬 Comunicação WhatsApp: Recebe mensagens via webhook e envia respostas diretamente para o cliente.
- 🧠 NLP Robusto: Classifica intenções das mensagens (Reservar, Cancelar, Consultar) usando Scikit-learn/TF-IDF.
- 📅 Extração de Entidades: Identifica Data, Horário, Duração e Sala desejada mesmo em linguagem natural.
- 🏢 Gestão de Múltiplos Recursos: Modelo Resource para gerenciar disponibilidade de várias salas simultaneamente.
- ⏱️ Verificação de Conflito: Detecta sobreposição de horários garantindo integridade da agenda.
- 🛠️ Painel Admin Completo: Interface Django Admin para gerenciar clientes, recursos e reservas.


## 🏗️ Stack Tecnológica


**Backend/API**	Python 3.10+ & Django	Sistema de reservas e manipulação de dados;

**NLP/IA**	Scikit-learn / Python Dateutil	Classificação de intenções e extração de dados temporais;

**Banco de Dados**	SQLite (Dev) / PostgreSQL (Prod)	Armazena clientes, recursos e reservas;

**API**	Django REST Framework (DRF)	Endpoint /webhook/ e API /api/bookings/
Mensageria	requests + variáveis de ambiente	Integração com APIs de WhatsApp (Meta Cloud, Z-API, etc.)

⚙️ Guia de Configuração e Execução Local
## ⚙️ Guia de Configuração e Execução Local
1️⃣ Preparação do Projeto e Clonar o repositório

```bash
git clone https://github.com/SeuUsuario/BootAgendamento.git
cd BootAgendamento
```

```bash
python -m venv venv

# Linux/Mac
source venv/bin/activate
# Windows
.\venv\Scripts\activate
```
    
Instalar dependências
```bash
pip install -r requirements.txt
```
### Configuração de Ambiente
2️⃣ Crie um arquivo .env na raiz do projeto com suas variáveis sensíveis:
#### Django
DEBUG=True
DJANGO_SECRET_KEY='sua_chave_secreta_deve_ser_unica'
ALLOWED_HOSTS=localhost,127.0.0.1,seusite.com

#### Banco de Dados (SQLite padrão)
#### DATABASE_URL=sqlite:///db.sqlite3

#### API de WhatsApp
WHATSAPP_API_URL='URL_DO_SEU_GATEWAY_DE_WHATSAPP'
WHATSAPP_API_TOKEN='TOKEN_DE_AUTENTICACAO_DO_SEU_GATEWAY'

#### Google Calendar (opcional)
GOOGLE_CALENDAR_ID='primary'
GOOGLE_SERVICE_ACCOUNT_FILE='client_secret.json'


⚠️ Atenção: O .env não deve ser versionado.

### Banco de Dados e Usuário Admin
3️⃣ Criar e aplicar migrações
```bash
python manage.py makemigrations
python manage.py migrate
```
### Criar superusuário para Django Admin
python manage.py createsuperuser

4️⃣ Treinamento do Modelo de NLP
```bash
python bookingbot/ia/train_model.py
```

5️⃣ Executando o Projeto
```bash
python manage.py runserver
```
Acesse o painel de administração: http://127.0.0.1:8000/admin/

Cadastre suas salas de estúdio em Resources (Ex.: Sala A, Estúdio Grande).

O bot está pronto para receber mensagens via webhook: http://127.0.0.1:8000/webhook/

## Documentação da API

#### Retorna todos os itens

| Endpoint   | Método      | Função                           |
| :---------- | :--------- | :---------------------------------- |
| /webhook/ | POST | Recebe e processa mensagens do WhatsApp. Configurar no provedor de API. |

#### Acessa o Admin

| Endpoint   | Método      | Função                           |
| :---------- | :--------- | :---------------------------------- |
| /admin/ | GET | Painel de controle para gestão de agendas, clientes e recursos. |

#### Integração Externa

| Endpoint   | Método      | Função                           |
| :---------- | :--------- | :---------------------------------- |
| /api/bookings/ | GET/POST | API REST para administração e integração externa de reservas. |

Abrir Issues para bugs ou sugestões.

Criar Fork e enviar Pull Requests com melhorias.

## 👤 Autor

Haroldo Alcobaças
GitHub

## 📜 Licença

MIT License